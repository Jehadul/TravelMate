<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:foreground="?android:selectableItemBackground"
    android:focusable="true"
    android:layout_marginLeft="16dp"
    android:layout_marginRight="16dp"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="10dp"
    app:cardElevation="5dp"
    app:cardCornerRadius="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorGrayLight"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/gallery_item_img"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scaleType="centerCrop"
            android:adjustViewBounds="true"
            android:src="@drawable/ic_photo"/>

        <TextView
            android:id="@+id/gallery_item_event_name"
            style="@style/AppTextViewCenter"
            android:textColor="@color/colorBlue"
            android:textStyle="italic"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="6dp"
            android:text="Travel Name"/>

        <TextView
            android:id="@+id/gallery_item_remark"
            style="@style/AppTextViewSmall"
            android:gravity="center"
            android:textStyle="italic"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="6dp"
            android:text="remark"/>

        <TextView
            android:id="@+id/gallery_item_post_date"
            style="@style/AppTextViewSmall"
            android:textColor="@color/colorAccent"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="6dp"
            android:layout_marginBottom="6dp"
            android:gravity="end"
            android:text="02 Dec, 2018"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>